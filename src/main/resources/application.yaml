spring:
  application:
    name: JobSearch
  datasource:
    url: jdbc:h2:file:./data/jobsearch;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: ozodbek
    password: 2006
  liquibase:
    change-log: classpath:db/changelog/changelog-master.yaml
  freemarker:
    expose-session-attributes: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8090